{
  "name": "ai-orchestrator-node",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Local helper scripts for the PowerShell AI orchestrator.",
  "scripts": {
    "check": "node --check gpt-prompter.mjs && node --check gpt-summarizer.mjs && node --check model-router.mjs"
  }
}
