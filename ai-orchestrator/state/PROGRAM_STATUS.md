# Program Status

The three-lane system is focused on conservative prediction-market relative value.

- `relative-value-scanner` is the primary lane and fastest path to useful paper candidates.
- `market-graph-consistency` is diagnostic-only structural hinting.
- `kalshi-weather-edge` is lower priority unless evidence-backed paper history appears.

Known state:

- Exact arb is preferred over predictive edge.
- Conservative fees killed a prior fake MLB candidate, which is considered progress.
- NBA/NHL fail closed unless settlement trust is reviewed.
- BTC/Fed need exact threshold and meeting/date-specific pipelines instead of broad matching.
- Graph outputs remain `WATCH` or `MANUAL_REVIEW`.
