# Next Steps

Relative-value lane:

- Fix live-readonly universe-specific snapshot handling.
- Continue exact universe inventory and ranking.
- Keep evaluator gates conservative.

Market-graph lane:

- Keep hint diff/schema/watchlist work diagnostic-only.
- Do not integrate graph hints into paper candidates.

Weather lane:

- Maintenance only.
- Continue evidence/replay hygiene only when bounded.

Orchestrator:

- Keep context compact.
- Tail dynamic logs.
- Generate recovery packets only after failure or compaction.
