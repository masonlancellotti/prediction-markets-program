# Blocker Analysis

Keep this concise. Move actionable work into `TASK_QUEUE.json` or `ROADMAP_BACKLOG.json`.

## relative_value

- BTC inventory exists, but pair matching is unrelated until exact source/date/threshold scope is proven.
- Fed/FOMC inventory exists, but matching is broad until meeting-specific pipelines exist.
- NHL same-scope pairs exist, but settlement/date drift blocks strict proof.
- MLB semantic path worked, but conservative Polymarket fees killed the prior fake candidate.

## graph

- Hints schema exists.
- Diff/watchlist output exists.
- Downstream consumer remains not allowed; graph outputs stay WATCH/MANUAL_REVIEW only.

## weather

- Lower priority unless strict target evidence appears.
- Weather edge requires observations, forecasts, settlement labels, and conservative replay history before paper promotion.
